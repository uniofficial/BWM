{
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"SPRING_PROFILES_ACTIVE=local ./gradlew bootRun\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
